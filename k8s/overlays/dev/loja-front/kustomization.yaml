resources:
  - ../../../base/namespace
  - ../../../base/loja-front
  - configmap.yaml
  - ingress.yaml

namespace: loja

patches:
  - path: patch.deployment.yaml

images:
  - name: loja-front:local
    newName: loja-front
    newTag: local
